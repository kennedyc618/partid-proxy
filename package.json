{
  "name": "partid-proxy",
  "version": "1.0.0",
  "description": "Proxy server for PartID — forwards image requests to Anthropic API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
